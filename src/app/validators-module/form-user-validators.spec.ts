import { FormUserValidators } from './form-user-validators';

describe('FormUserValidators', () => {
  it('should create an instance', () => {
    expect(new FormUserValidators()).toBeTruthy();
  });
});
